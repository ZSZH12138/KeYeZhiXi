# 课业智析使用说明

## 1. 启动和停止

### 可执行版

1. 将压缩包完整解压到有读写权限的目录，不要直接在压缩包内运行。
2. 双击 `启动课业智析.cmd`。
3. 首次启动会自动创建数据库并执行升级，然后提示设置管理员账号和密码。
4. 输入完成后，平台、知识解析 Worker 和事件 Worker 会启动，浏览器自动打开 <http://127.0.0.1:8000/accounts/login/>。
5. 结束使用时双击 `停止课业智析.cmd`。

首次密码至少需要 12 个字符。密码只保存为不可逆哈希，不会写入说明文件或明文凭据文件。

### 源码版

源码版需要 Conda 和 Python 3.12：

```powershell
conda create --name course-insight python=3.12 -y
conda activate course-insight
python -m pip install --constraint requirements/ci-constraints.txt -e ".[dev,intent]"
```

安装完成后，同样可以双击项目根目录中的 `启动课业智析.cmd`。如果 Conda 不在 PATH 中，也可以在已激活的 `course-insight` 环境中运行：

```powershell
python -m course_insight.desktop_launcher start
```

停止命令：

```powershell
python -m course_insight.desktop_launcher stop
```

## 2. 首次配置

首次启动后先使用刚设置的管理员账号登录。

1. 创建至少一个教师账号；
2. 创建需要参加课程的学生账号；
3. 将账号名和初始密码安全地分别交给对应用户；
4. 退出管理员账号。

管理员只管理账号，不能代替教师创建课程或修改教学数据。

## 3. 教师使用流程

1. 使用教师账号登录；
2. 创建课程和班级，填写清晰的课程名与班级名；
3. 按学生账号名把学生加入班级；
4. 进入班级的 DeepSeek 设置，保存该班级使用的 API 密钥；
5. 上传课程资料和题库文件并确认处理；
6. 等待处理进度完成，检查知识点与题目解析结果；
7. 让学生开始诊断测评、练习或阶段评测；
8. 在班级分析中查看整体情况和学生个体画像；
9. 在“建议教师复核”中处理低置信度主观题评分。

支持的常见课程文件包括 PDF、Word、PowerPoint 和纯文本。旧格式 PowerPoint 的转换依赖本机安装的 Microsoft PowerPoint；无法转换时请另存为 `.pptx` 后重新上传。

## 4. 学生使用流程

1. 使用管理员分配的学生账号登录；
2. 从课程列表进入本人已加入的班级；
3. 选择诊断测评、随心练习、阶段评测或错题订正；
4. 提交后查看逐题结果、课程依据和学习反馈；
5. 对错题进行订正，或在答疑入口继续提问。

学生只能查看本人的作答和画像，不能查看其他学生的信息。

## 5. DeepSeek 设置

DeepSeek 密钥由教师按班级单独设置：

- 一个班级的密钥不会自动用于另一个班级；
- 未配置密钥时，知识解析、主观题语义评分和智能答疑会给出明确提示；
- 密钥加密后保存在本机 `runtime` 中；
- 不要在聊天记录、截图或日志中公开 API 密钥。

## 6. 数据备份与迁移

单机版的业务数据位于程序目录的 `runtime` 中。

备份步骤：

1. 双击 `停止课业智析.cmd`；
2. 确认三个进程已经退出；
3. 复制整个 `runtime` 文件夹到安全位置；
4. 如需恢复，先停止平台，再用备份覆盖同名 `runtime` 文件夹。

不要在平台运行时直接复制 SQLite 数据库，否则可能得到不一致的备份。

## 7. 常见问题

### 双击后浏览器没有打开

手动访问 <http://127.0.0.1:8000/accounts/login/>。如果仍无法访问，查看启动窗口和 `runtime/logs` 中的错误信息。

### 提示 8000 端口被占用

先运行 `停止课业智析.cmd`。如果仍被占用，请关闭占用 8000 端口的其他程序后重新启动。

### 知识文件一直显示排队中

确认知识解析 Worker 窗口仍在运行，并检查对应班级是否已正确配置 DeepSeek 密钥。

### 主观题进入教师复核

这是正常的安全机制。模型置信度低于当前评分规则阈值或模型评分不可用时，系统不会直接更新最终画像，教师确认后才会生效。

### 忘记管理员密码

源码版可以在 Conda 环境中执行：

```powershell
python manage.py changepassword 管理员账号
```

可执行版请保留 `runtime` 数据并联系系统维护人员处理，不要删除数据库文件。

## 8. 使用边界

- 可执行版定位为本地桌面或可信局域网部署；
- 不要把本地开发服务直接暴露到公网；
- 公网、多人并发或正式生产部署应使用 PostgreSQL、受支持的 WSGI/ASGI 服务、反向代理、HTTPS、备份和监控；
- 处理真实学生数据前，应遵守学校的数据保护和授权要求。
